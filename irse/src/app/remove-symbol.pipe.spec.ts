import { RemoveSymbolPipe } from './remove-symbol.pipe';

describe('RemoveSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
